name: Remove Chinese code comments from PR

on:
  pull_request:
    branches:
      - master

permissions:
  contents: write

jobs:
  cleanup:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Remove Chinese comments only
        shell: bash
        run: |
          python - <<'PY'
          from __future__ import annotations

          import ast
          import io
          import re
          import tokenize
          from pathlib import Path

          ROOT = Path.cwd()
          CJK = re.compile(r"[\u3400-\u4dbf\u4e00-\u9fff\uf900-\ufaff]")
          EXCLUDED_PARTS = {".git", "artifacts", "__pycache__"}


          def contains_cjk(text: str) -> bool:
              return bool(CJK.search(text))


          def line_offsets(text: str) -> list[int]:
              offsets = [0]
              for line in text.splitlines(keepends=True):
                  offsets.append(offsets[-1] + len(line))
              return offsets


          def docstring_spans(text: str) -> list[tuple[int, int]]:
              tree = ast.parse(text)
              offsets = line_offsets(text)
              spans: list[tuple[int, int]] = []
              owners = (ast.Module, ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)
              for owner in ast.walk(tree):
                  if not isinstance(owner, owners):
                      continue
                  body = getattr(owner, "body", None)
                  if not body:
                      continue
                  first = body[0]
                  if not (
                      isinstance(first, ast.Expr)
                      and isinstance(first.value, ast.Constant)
                      and isinstance(first.value.value, str)
                      and contains_cjk(first.value.value)
                  ):
                      continue
                  start_line = first.lineno - 1
                  end_line = first.end_lineno or first.lineno
                  start = offsets[start_line]
                  end = offsets[end_line] if end_line < len(offsets) else len(text)
                  spans.append((start, end))
              return spans


          def python_comment_spans(text: str) -> list[tuple[int, int]]:
              offsets = line_offsets(text)
              spans: list[tuple[int, int]] = []
              for token in tokenize.generate_tokens(io.StringIO(text).readline):
                  if token.type != tokenize.COMMENT or not contains_cjk(token.string):
                      continue
                  start = offsets[token.start[0] - 1] + token.start[1]
                  end = offsets[token.end[0] - 1] + token.end[1]
                  spans.append((start, end))
              return spans


          def apply_deletions(text: str, spans: list[tuple[int, int]]) -> str:
              merged: list[list[int]] = []
              for start, end in sorted(spans):
                  if merged and start <= merged[-1][1]:
                      merged[-1][1] = max(merged[-1][1], end)
                  else:
                      merged.append([start, end])
              for start, end in reversed(merged):
                  text = text[:start] + text[end:]
              return text


          def clean_python(path: Path) -> bool:
              original = path.read_text(encoding="utf-8")
              spans = docstring_spans(original) + python_comment_spans(original)
              if not spans:
                  return False
              cleaned = apply_deletions(original, spans)
              ast.parse(cleaned)
              path.write_text(cleaned, encoding="utf-8")
              return cleaned != original


          def shell_comment_index(line: str) -> int | None:
              single = False
              double = False
              escaped = False
              for index, char in enumerate(line):
                  if escaped:
                      escaped = False
                      continue
                  if char == "\\" and not single:
                      escaped = True
                      continue
                  if char == "'" and not double:
                      single = not single
                      continue
                  if char == '"' and not single:
                      double = not double
                      continue
                  if char != "#" or single or double:
                      continue
                  if index == 0 or line[index - 1].isspace() or line[index - 1] in ";|&()":
                      return index
              return None


          def clean_shell(path: Path) -> bool:
              original = path.read_text(encoding="utf-8")
              output: list[str] = []
              changed = False
              for number, line in enumerate(original.splitlines(keepends=True), start=1):
                  if number == 1 and line.startswith("#!"):
                      output.append(line)
                      continue
                  index = shell_comment_index(line)
                  if index is None or not contains_cjk(line[index:]):
                      output.append(line)
                      continue
                  newline = "\n" if line.endswith("\n") else ""
                  output.append(line[:index].rstrip() + newline)
                  changed = True
              cleaned = "".join(output)
              if changed:
                  path.write_text(cleaned, encoding="utf-8")
              return changed


          def remaining_python_comments(path: Path) -> list[str]:
              text = path.read_text(encoding="utf-8")
              remaining: list[str] = []
              tree = ast.parse(text)
              owners = (ast.Module, ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)
              for owner in ast.walk(tree):
                  if not isinstance(owner, owners):
                      continue
                  body = getattr(owner, "body", None)
                  if not body:
                      continue
                  first = body[0]
                  if (
                      isinstance(first, ast.Expr)
                      and isinstance(first.value, ast.Constant)
                      and isinstance(first.value.value, str)
                      and contains_cjk(first.value.value)
                  ):
                      remaining.append(f"docstring at line {first.lineno}")
              for token in tokenize.generate_tokens(io.StringIO(text).readline):
                  if token.type == tokenize.COMMENT and contains_cjk(token.string):
                      remaining.append(f"comment at line {token.start[0]}")
              return remaining


          changed_files: list[str] = []
          code_files = sorted(
              path
              for path in ROOT.rglob("*")
              if path.is_file()
              and path.suffix in {".py", ".sh"}
              and not any(part in EXCLUDED_PARTS for part in path.parts)
          )

          for path in code_files:
              changed = clean_python(path) if path.suffix == ".py" else clean_shell(path)
              if changed:
                  changed_files.append(path.relative_to(ROOT).as_posix())

          failures: list[str] = []
          for path in code_files:
              if path.suffix == ".py":
                  for item in remaining_python_comments(path):
                      failures.append(f"{path.relative_to(ROOT)}: {item}")
              else:
                  for number, line in enumerate(path.read_text(encoding="utf-8").splitlines(), start=1):
                      if number == 1 and line.startswith("#!"):
                          continue
                      index = shell_comment_index(line)
                      if index is not None and contains_cjk(line[index:]):
                          failures.append(f"{path.relative_to(ROOT)}: comment at line {number}")

          if failures:
              raise SystemExit("Chinese comments remain:\n" + "\n".join(failures))

          print("Changed code files:")
          for path in changed_files:
              print(path)
          print(f"Total changed code files: {len(changed_files)}")
          PY

          rm -f .github/workflows/remove_chinese_comments.yml
          rm -f .github/workflows/remove_chinese_comments_pr.yml

          python -m compileall -q gwn scripts
          while IFS= read -r script; do
            bash -n "${script}"
          done < <(find scripts -type f -name '*.sh' | sort)
          git diff --check

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No Chinese code comments required removal."
            exit 0
          fi
          git commit --amend --no-edit
          git push --force-with-lease origin "HEAD:${GITHUB_HEAD_REF}"
